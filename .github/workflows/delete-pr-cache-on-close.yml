name: Delete PR Action Cache On PR Close
on:
  pull_request_target:
    types: [closed]
jobs:
  delete-pr-cache:
    runs-on: ubuntu-latest
    permissions:
      actions: write

    steps:
      - uses: toshimaru/delete-action-cache@v1.0.1
